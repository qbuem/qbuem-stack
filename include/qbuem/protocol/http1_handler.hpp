#pragma once

/**
 * @file qbuem/protocol/http1_handler.hpp
 * @brief HTTP/1.1 connection handler (canonical location).
 * @ingroup qbuem_http1_handler
 *
 * Since v1.0 all protocol handlers are located under `qbuem/protocol/`.
 * `qbuem/server/http1_handler.hpp` is kept for backwards compatibility.
 *
 * @see qbuem/server/http1_handler.hpp (compatibility header)
 */

#include <qbuem/server/http1_handler.hpp>
