#pragma once

/**
 * @file qbuem/protocol/websocket_handler.hpp
 * @brief RFC 6455 WebSocket 핸들러 (정식 위치).
 * @ingroup qbuem_websocket_handler
 *
 * v1.0부터 프로토콜 핸들러는 `qbuem/protocol/` 아래에 위치합니다.
 * 하위 호환을 위해 `qbuem/server/websocket_handler.hpp`도 유지됩니다.
 *
 * @see qbuem/server/websocket_handler.hpp (호환 헤더)
 */

#include <qbuem/server/websocket_handler.hpp>
